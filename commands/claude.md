---
description: Route Claude questions & plan approvals back to Claude Code first
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/run.sh --mode claude)
---

!`"${CLAUDE_PLUGIN_ROOT}/bin/run.sh" --mode claude`

Relay the switch confirmation above to the user in one short line. Do nothing else.
