---
description: Route Claude questions & plan approvals to the Telegram bot first
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/run.sh --mode bot)
---

!`"${CLAUDE_PLUGIN_ROOT}/bin/run.sh" --mode bot`

Relay the switch confirmation above to the user in one short line. Do nothing else.
